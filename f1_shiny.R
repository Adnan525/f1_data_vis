library(shiny)
ui <- fluidPage(
  "F1 Analysis"
)
server <- function(input, output, session){
  
}
library(shiny)

ui <- fluidPage(
  "F1 Analysis"
)

server <- function(input, output, session) {
  
}

shinyApp(ui, server)